<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="<%= root_path %>">Egresso</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      
      <li class="nav-item">
        <a class="nav-link" href="<%= courses_path %>">Cursos</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= coordinators_path %>">Coordenadores</a>
      </li>
      <li class="nav-item">
        <a class="nav-link disabled" href="<%= students_path %>">Estudantes</a>
      </li>
      <li class="nav-item">
      <% if student_signed_in? %>
          <%= current_student.nome %> - <%= link_to "Sair", destroy_student_session_path, method: :delete %>
      <% elsif coordinator_signed_in?  %>
          <%= current_coordinator.nome%> - <%= link_to "Sair", destroy_coordinator_session_path, method: :delete %>
      <% else %>
          <%= link_to "Entrar como estudante", new_student_session_path %>
          <%= link_to "Entrar como coordenador", new_coordinator_session_path %>
      <% end %>
        
      </li>
    </ul>
  </div>
</nav>