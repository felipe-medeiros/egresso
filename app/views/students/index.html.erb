<p id="notice"><%= notice %></p>

<h2>Estudantes</h2>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Email</th>
      <th>Curso</th>
      <th>Conclusão</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @students.each do |student| %>
      <% if (student.perfil).to_i == 1 && (student.homologado).to_i == 1 %>
        <tr>
          <td><%= student.nome %></td>
          <td><%= student.email %></td>        
          <td><%= student.course.nome %></td>
          <td><%= student.conclusion.nome %></td>
           <td><%= link_to 'Detalhes', student %></td>
          <!-- <td><%= link_to 'Edit', edit_student_path(student) %></td>
          <td><%= link_to 'Destroy', student, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
        </tr>
      <%end%>
    <% end %>
  </tbody>
</table>

<br>
